# iOS 逆向 2026 Q2 完整实施计划

> 主线：公司 IM 测试包 → 注册协议可见 → 指纹/验证码改包对照 → 服务端防御建议  
> 适用环境：同 Q1（Mac mini M2、Xcode 26.6、iPhone XR iOS 18.5 Dopamine、Frida 17）  
> 当前日期：2026-09-12  
> Q1 计划：[`iOS逆向_2026Q1完整实施计划.md`](iOS逆向_2026Q1完整实施计划.md)  
> Q1 复盘：[`notes/Q1-review.md`](notes/Q1-review.md)  
> 对象：**本公司 IM** 测试包（工作名 `myim.ipa`）、测试服、测试号  
> **不是** WhatsApp，**不写** 注册机，**不打** 生产用户

---

## 0. 当前状态

### 0.1 Q1 已具备（不再重做）

- [x] 越狱 XR + `frida-ps -U`
- [x] mitmproxy、Ghidra、LLDB、Theos 最小闭环
- [x] SignDemo：HMAC 重放 A–F、水平 IDOR 修复对照、JailbreakCheck FP/FN
- [x] 结论：XOR / 越狱检测 / 客户端指纹提高静态成本，挡不住 hook 和协议层改字段

### 0.2 当前未完成（Q2 入口）

当前未完成：进入 **Q2-W1：授权、测包与 cryptid**。

开干清单：[`iOS逆向实施步骤/21-Q2-公司IM注册链路-详细执行步骤.md`](iOS逆向实施步骤/21-Q2-公司IM注册链路-详细执行步骤.md)（执行 1～6 覆盖 M4–M6 核心路径）。  
仅当测包 `cryptid=1` 且需要静态分析时：[`21.1-Q2-本公司IPA砸壳-详细执行步骤.md`](iOS逆向实施步骤/21.1-Q2-本公司IPA砸壳-详细执行步骤.md)。

### 0.3 路线决策

1. **主线是公司 IM 测试环境，不是商店第三方。**
2. **先协议、后加固。** 注册机通常复现取号 → 短信 → 校验 → 落户，不必把加固 so 翻完。
3. **指纹是信号不是密钥。** 已观察到可改；Q2 写成可复现对照（改字段 / hook getter → HTTP 是否仍成功）。
4. **优先未加密测包。** 商店 IPA 常 `cryptid=1`；Debug / Ad-hoc / 企业测包才方便 Ghidra。
5. **防御写给服务端。** DeviceCheck / App Attest、短信一码一号、号段/IP 限速；不靠再 XOR 一层客户端字符串。

---

## 1. 总目标与验收方式

### 1.1 季度目标

```text
公司授权 + 测试号
    ↓
确认 myim.ipa / 测包 cryptid
    ↓
观察注册相关 HTTPS / WS
    ↓
Frida 定位指纹与签名生成点
    ↓
改指纹、重放验证码（只打测试服）
    ↓
判断哪一步只信客户端（注册机因此能过）
    ↓
防御报告（服务端可拦什么）
```

要回答的三个问题：

1. 注册机是否必须翻开加固二进制，还是只复现几步协议。
2. 机器指纹在哪个字段；改了之后服务端是否仍业务成功（不只看 HTTP 200）。
3. 哪些门槛只信客户端上报，哪些服务端可独立验证。

### 1.2 里程碑

| 里程碑 | 周期 | 通过标准 | 主要产物 |
|---|---:|---|---|
| M4 包与授权就绪 | Q2-W1–W2 | 书面授权、测试号、cryptid 已记录、App 能在 XR 跑 | `notes/q2-week-01-ipa.md` |
| M5 注册协议可见 | Q2-W3–W5 | 取号/短信/校验/落户的 host、路径、关键字段表 | `reports/myim-register-traffic.md` |
| M6 对照与防御 | Q2-W6–W12 | 指纹改包表 + 验证码重放表 + 服务端建议 | `reports/myim-register-defense.md` |

### 1.3 通过规则（同 Q1）

- **可运行**：测包能装、请求能打到测试服。
- **可解释**：能说明字段从哪来、改哪、服务端看什么。
- **可复现**：隔天同一测试号流程仍能得到同类结果。

---

## 2. 工作区和产物规范

Q1 的 SignDemo / reports / scripts 保留。Q2 增量：

```text
iOS-Reverse-Lab/
├── iOS逆向_2026Q1完整实施计划.md
├── iOS逆向_2026Q2完整实施计划.md
├── notes/
│   ├── Q1-review.md
│   ├── q2-week-01-ipa.md
│   └── ...
├── reports/
│   ├── myim-register-traffic.md
│   ├── myim-fingerprint.md
│   └── myim-register-defense.md
├── scripts/
│   ├── frida/observe_myim_register.js    # 只观察，不写注册机
│   └── reproduce/                        # 仅测试服、测试号
└── artifacts/                            # gitignore：ipa、证书、真号
```

- 周笔记：`notes/q2-week-XX-topic.md`
- **不要** 把 `myim.ipa`、真手机号、生产 Token 提交进 git
- 报告只含测试环境；不附接码平台用法、不附可运行的注册脚本

### 2.1 包从哪来

| 来源 | cryptid | Q2 是否优先 |
|---|---|---|
| 公司 Debug / Ad-hoc / 企业测包 | 多为 0 | **是** |
| 商店 IPA | 常 1（FairPlay） | 否，未解壳不要当静态主路径 |

```bash
otool -l MyIM.app/MyIM | grep -A2 cryptid
```

---

## 3. 明确不做

- 不实现、不分发注册机
- 不接接码平台打生产、不刷真实用户库
- 不把 WhatsApp 或其他商店 IM 当实验对象
- 不把「Hook 掉指纹」写成已攻破生产
- 不默认 `ptrace` / 全局 SSL Kill
- 不把来路不明的「破解版注册机」拿来当教材运行

---

## 4. 周计划（Q2-W1–W12）

### Q2-W1：授权与测包

- 书面范围：测试服、测试号、禁止生产数据
- 拿到 `myim.ipa` 或 Xcode 测包；记录 Bundle ID、版本、`cryptid`
- 产物：`notes/q2-week-01-ipa.md`

### Q2-W2：装上 XR、确认进程

- 免费账号 3 App 上限：必要时先卸练习包
- `frida-ps -Uai` 能看到 IM
- 反调试若杀 Frida：只在测试包上观察，不先对抗

### Q2-W3：流量面（注册相关）

- 对照 Q1 W4/W5：HTTPS / WS，只盯注册
- Pinning 若挡住 mitm：测试包可关钉扎或只观察，不上全局 SSL Kill
- 产物草稿：host 列表

### Q2-W4：注册链路字段

画出四步（名称按实际改）：

```text
取号 / 发短信 → 提交验证码 → 落户 / 登录 → 后续 IM 长连接
```

每步记录：方法、路径、指纹字段、签名字段、业务成功条件（不要只看 200）

### Q2-W5：指纹生成点

- 对照 Q1 W7/W11：Frida 看 getter / 上报前的值
- 常见：IDFV、Keychain UUID、自拼 deviceId、机型、系统版本
- 产物：`reports/myim-fingerprint.md` 初稿（生成点，尚未改包）

### Q2-W6：指纹改包对照（M5→M6）

| 步骤 | 操作 | 记录 |
|---|---|---|
| A | 正常测试号注册/登录 | 业务成功？ |
| B | 只改上报指纹，其它不变 | 是否仍成功 |
| C | hook getter 再走一遍 | 与 B 是否同类 |

已有「似乎能改」：本周要变成表。改了仍成功 → 服务端只信客户端。

### Q2-W7：验证码与重放

- 对照 Q1 W17：原样重放、过期、一码多用
- 只打测试服；测试号短信即可，不要接码平台

### Q2-W8：加固挡得住协议吗

- 混淆 / 越狱检测 / XOR 是否改变 Q2-W4 的字段
- 预期：静态更难看，运行时 hook 仍能看到明文（同 Q1 W19）

### Q2-W9：服务端可验证信号

把「只信客户端」改成建议：

- 短信：一码一号、短时窗、号段/IP 限速
- iOS：**DeviceCheck / App Attest**（Apple 证明，不是 JSON 里的 UUID）
- 同一指纹短时间对多个测试号成功 → 拒绝

### Q2-W10：防御报告

- `reports/myim-register-traffic.md`
- `reports/myim-fingerprint.md`
- `reports/myim-register-defense.md`
- 写清：注册机不必翻完加固包；指纹可改是因为上报值可替换

### Q2-W11：测试服修复验证（若能改后端）

- 对照 Q1 W18：同一请求，修前成功、修后拒绝
- 改不了生产后端就只写建议，不要对生产试

### Q2-W12：Q2 复盘

- `notes/Q2-review.md`
- 哪些结论只在测试包成立

---

## 5. 验收标准

### M4

- [ ] 授权与测试号到位
- [ ] `cryptid` 已记录；主分析包尽量为 0

### M5

- [ ] 注册四步有 host / 路径 / 关键字段
- [ ] 指纹字段有名字、出现在哪次请求

### M6

- [ ] 指纹对照表：改前 / 改后 / 业务是否成功
- [ ] 验证码重放至少一行记录
- [ ] 防御报告区分客户端加固 vs 服务端可拦
- [ ] 报告不含注册机、不含生产数据

---

## 6. 和 Q1 的衔接

| Q1 | Q2 用在哪 |
|---|---|
| W4 Pinning / mitm | 注册 HTTPS |
| W7 Frida ObjC | 指纹 getter |
| W11/W12 签名定位 | 注册请求是否有客户端 sign |
| W17 重放 A–F | 改设备 ID、重放验证码 |
| W18 IDOR | 本季度主线是注册，不是聊天越权（可记到 Q3） |
| W19 检测面 | 加固挡 strings，挡不住 hook |

Q1 SignDemo：没签名 401，读别人订单 403。  
Q2 问你们 IM：**改指纹是否仍能注册成功。**
