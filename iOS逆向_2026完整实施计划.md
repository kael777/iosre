# iOS 逆向 2026 完整实施计划

> 主线：抓包与协议逆向 → Frida 动态分析 → Mach-O/静态分析 → 自建靶场漏洞研究  
> 适用环境：Mac mini 2023（Apple M2、macOS 26.6.2、Xcode 26.6）+ iPhone XR（A12、arm64e、iOS 18.5）  
> 当前日期：2026-09-10  
> 学习对象：自己的 XR、自建 App/后端、OWASP 和 DVIA 等授权靶场  
> 配套文档：`iOS逆向环境搭建_安装清单.md`

---

## 0. 当前状态

### 0.1 已完成

- [x] Mac、芯片和系统版本已确认。
- [x] Homebrew、mitmproxy、Frida 客户端和 Ghidra 安装路线已确定。
- [x] iPhone XR 已完成 Dopamine 安装/激活，采用路线 A：越狱 + Frida。
- [x] 下载 Dopamine 时已确认需要 Mac 开启全局 VPN。
- [x] `frida-ps -U` 已经能够看到 XR 上的进程。
- [x] 已确认当前不把 AppSync 作为 iOS 18.5 的主安装路线。
- [x] 已确认 UnCrackable L1 使用 OWASP 源码工程编译安装，不再依赖过期 IPA。
- [x] 已定位 Xcode 26 编译旧工程时的 `libarclite` 问题。

### 0.2 已完成（原“当前未完成”六项）

截至 2026-09-10，[`00.2-当前未完成-详细执行步骤.md`](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md) 中的六项均已完成：

- [x] [执行 1：确认 App 标识符](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-1确认-app-标识符)
- [x] [执行 2：创建 SignDemo 并完成第一次 Frida attach/spawn](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-2创建-signdemo-并完成第一次-frida-attachspawn)
- [x] [执行 3：建立本地后端、签名协议和测试数据](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-3建立本地后端签名协议和测试数据)
- [x] [执行 4：编译安装源码版 UnCrackable L1](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-4编译安装源码版-uncrackable-l1)
- [x] [执行 5：完成 UnCrackable L1 静态定位和动态分析](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-5完成-uncrackable-l1-静态定位和动态分析)
- [x] [执行 6：完成第一份 SignDemo 抓包分析报告](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md#执行-6完成第一份-signdemo-抓包分析报告)

- [x] [W4 TLS Pinning 详细执行步骤](iOS逆向实施步骤/04-W4-TLS-Pinning-详细执行步骤.md)（2026-09-10 完成，笔记：[`notes/week-04-pinning.md`](notes/week-04-pinning.md)）

- [x] [W5 协议可见性详细执行步骤](iOS逆向实施步骤/05-W5-协议可见性-详细执行步骤.md)（2026-09-10 完成，笔记：[`notes/week-05-protocols.md`](notes/week-05-protocols.md)）

- [x] [W6 M1 综合验收详细执行步骤](iOS逆向实施步骤/06-W6-M1综合验收-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-06-m1.md`](notes/week-06-m1.md)）

- [x] [W7 Frida 与 ObjC Runtime 详细执行步骤](iOS逆向实施步骤/07-W7-Frida-ObjC-Runtime-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-07-frida-runtime.md`](notes/week-07-frida-runtime.md)）

- [x] [W8 Native Hook 与 UnCrackable L1 详细执行步骤](iOS逆向实施步骤/08-W8-Native-Hook-UnCrackable-L1-详细执行步骤.md)（2026-09-11 完成，报告：[`reports/uncrackable-l1.md`](reports/uncrackable-l1.md)）

- [x] [W9 Mach-O 与 Ghidra 详细执行步骤](iOS逆向实施步骤/09-W9-Mach-O-Ghidra-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-09-mach-o.md`](notes/week-09-mach-o.md)，报告：[`reports/signdemo-static-location.md`](reports/signdemo-static-location.md)）

- [x] [W10 脱壳概念与静态-动态对照详细执行步骤](iOS逆向实施步骤/10-W10-脱壳概念-静态动态-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-10-static-dynamic.md`](notes/week-10-static-dynamic.md)，报告：[`reports/signdemo-static-dynamic.md`](reports/signdemo-static-dynamic.md)）

- [x] [W11 签名混淆与重新定位详细执行步骤](iOS逆向实施步骤/11-W11-签名混淆-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-11-signature-location.md`](notes/week-11-signature-location.md)）

- [x] [W12 M2 签名逆向综合详细执行步骤](iOS逆向实施步骤/12-W12-M2签名逆向综合-详细执行步骤.md)（2026-09-11 完成，报告：[`reports/signature-reproduction.md`](reports/signature-reproduction.md)，笔记：[`notes/week-12-m2.md`](notes/week-12-m2.md)）

- [x] [W13 LLDB 与断点详细执行步骤](iOS逆向实施步骤/13-W13-LLDB-断点-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-13-lldb.md`](notes/week-13-lldb.md)，崩溃记录：[`reports/signdemo-crash-lldb.md`](reports/signdemo-crash-lldb.md)）

- [x] [W14 Theos 与最小 Tweak 详细执行步骤](iOS逆向实施步骤/14-W14-Theos-最小Tweak-详细执行步骤.md)（2026-09-11 完成，笔记：[`notes/week-14-theos.md`](notes/week-14-theos.md)）

- [x] [W15 DVIA 与客户端存储详细执行步骤](iOS逆向实施步骤/15-W15-DVIA-客户端存储-详细执行步骤.md)（2026-09-11 完成，报告：[`reports/dvia-storage.md`](reports/dvia-storage.md)、[`reports/insecureapp-storage.md`](reports/insecureapp-storage.md)）

- [x] [W16 URL Scheme、WebView 与本地认证详细执行步骤](iOS逆向实施步骤/16-W16-URL-WebView-本地认证-详细执行步骤.md)（2026-09-11 完成）

- [x] [W17 API 业务逻辑与重放详细执行步骤](iOS逆向实施步骤/17-W17-API重放-详细执行步骤.md)（2026-09-11 完成，报告：[`reports/local-api-replay.md`](reports/local-api-replay.md)）

- [x] [W18 越权、IDOR 与修复验证详细执行步骤](iOS逆向实施步骤/18-W18-越权-IDOR-详细执行步骤.md)（2026-09-11 完成，报告：[`reports/local-api-idor.md`](reports/local-api-idor.md)）

当前未完成：进入主计划 **W19：反调试、越狱检测和反分析**。

- [ ] [W19 反调试、越狱检测与反分析详细执行步骤](iOS逆向实施步骤/19-W19-反调试-越狱检测-详细执行步骤.md)

> 00.2 的操作拆分在 [`iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md`](iOS逆向实施步骤/00.2-当前未完成-详细执行步骤.md)。W4–W18 的手册在 `iOS逆向实施步骤/`。W19 在 [`19-W19-反调试-越狱检测-详细执行步骤.md`](iOS逆向实施步骤/19-W19-反调试-越狱检测-详细执行步骤.md)。本计划保留路线、依赖和验收入口。

### 0.3 重要路线决策

1. **主线是越狱设备 + 自建 App + 自建后端。**
2. **UnCrackable 不再依赖过期 IPA 直接安装。** 已使用 `OWASP/mas-crackmes` 源码，通过 Xcode 和免费 Apple Account 编译安装到 XR；Bundle Identifier 为 `com.weideshun.uncrackable1`。
3. **AppSync 暂不作为主线。** 官方源当前访问不稳定，官方 AppSync Unified 116.0 的包元数据对固件版本有限制，不适合把它当成 iOS 18.5 的必经步骤。
4. **mitmproxy 已足够完成抓包主线。** Charles、Proxyman 只作为可选 GUI 对比工具，不重复安装。
5. **免费 Apple Account 足够编译自己的练习 App。** 不要求付费 Apple Developer，但签名有效期和能力有限。

---

## 1. 总目标与验收方式

### 1.1 年度目标

在 20 周内完成以下闭环：

```text
自建 App 发起请求
    ↓
mitmproxy 观察 HTTP/HTTPS/WS 流量
    ↓
Frida 定位客户端参数和签名生成点
    ↓
Ghidra/LLDB 辅助定位实现
    ↓
Python 离线复现自建协议
    ↓
在自建靶场验证重放、越权、存储和客户端信任问题
    ↓
形成可复查的技术报告
```

### 1.2 里程碑

| 里程碑 | 周期 | 通过标准 | 主要产物 |
|---|---:|---|---|
| M0 环境可用 | W1 | Xcode、mitmproxy、Frida、Ghidra 可运行 | 环境状态表 |
| M1 抓包主线 | W2-W6 | HTTPS、Pinning、WS、HTTP/2、QUIC、gRPC 有实操记录 | SignDemo 流量分析报告 |
| M2 逆向闭环 | W7-W12 | 从流量定位到签名并用脚本离线复现 | 签名逆向报告和脚本 |
| M3 漏洞研究入门 | W13-W20 | 自建靶场完成 3 份漏洞报告 | 漏洞报告集和季度复盘 |

### 1.3 通过规则

每周不以“看完视频”作为完成标准，只以以下三类结果判断：

- **可运行**：命令能执行，App 能启动，后端能响应。
- **可解释**：能说明输入、输出、关键组件和失败原因。
- **可复现**：隔天重新操作，仍能得到相同结果。

如果某周的验收未通过，下一周先保留该周的补课任务，不要直接跳到下一阶段。

---

## 2. 工作区和产物规范

建议在本机建立一个独立工作目录，不把密码、真实 Token 或第三方 App 数据放进去。

```text
iOS-Reverse-Lab/
├── SignDemo/
│   ├── app/
│   ├── backend/
│   ├── scripts/
│   └── README.md
├── InsecureApp/
├── notes/
│   ├── week-01-foundation.md
│   ├── week-02-mitmproxy.md
│   ├── week-03-frida.md
│   └── ...
├── scripts/
│   ├── frida/
│   ├── mitmproxy/
│   └── reproduce/
├── reports/
│   ├── SignDemo-traffic-analysis.md
│   ├── signature-reproduction.md
│   ├── uncrackable-l1.md
│   └── dvia-storage.md
└── captures/
    └── .gitkeep
```

### 2.1 文件命名规则

- 周笔记：`notes/week-XX-topic.md`
- 抓包报告：`reports/<target>-traffic-analysis.md`
- Frida 脚本：`scripts/frida/<purpose>.js`
- 可重复脚本：`scripts/reproduce/<purpose>.py`
- 截图和脱敏流量：`captures/<date>-<topic>/`

### 2.2 每份笔记必须包含

```markdown
# 标题

## 环境
- 日期：
- 设备：
- App 版本：
- 后端版本：

## 目标

## 操作步骤

## 观察结果

## 失败与原因

## 结论

## 下一步
```

---

## 3. 固定周节奏

每周按 20 小时左右安排。可以根据工作情况缩短，但必须保留验收和记录。

| 日程 | 建议内容 | 结果 |
|---|---|---|
| 周一 | 理论和环境确认，阅读本周输入材料 | 写出本周问题清单 |
| 周二 | 按教程完成最小实验 | 产生第一次可观察结果 |
| 周三 | 在自己的 App/后端中重复实验 | 形成可控变量 |
| 周四 | 排查失败、补命令和记录 | 固化排障步骤 |
| 周五 | 独立重做，不看笔记 | 判断是否真正掌握 |
| 周六 | 2-4 小时综合实操 | 产出脚本或报告 |
| 周日 | 复盘和整理 | 更新验收表、决定是否进入下一周 |

### 3.1 每次实操的固定顺序

```text
1. 确认设备和 App 状态
2. 记录目标和预期结果
3. 只改一个变量
4. 保存终端输出、截图或脱敏流量
5. 解释结果
6. 恢复环境
7. 把结论写进周笔记
```

### 3.2 每周结束时必须回答

- 本周我能独立运行什么命令？
- 哪个结果是我亲自验证的？
- 哪个结论只是看别人文章得出的？
- 失败时我首先检查哪三个地方？
- 下周的输入依赖本周哪项产物？

---

## 4. SignDemo 固定规格

SignDemo 是整个计划的主要自建靶场。它不追求产品功能，而是追求可控、可观察、可重复。

### 4.1 App 规格

- Product Name：`SignDemo`
- Bundle Identifier：`com.weideshun.SignDemo`
- 设备：iPhone XR
- 建议语言：Swift 或 Objective-C 均可；关键控制器建议使用 Objective-C，方便学习 ObjC Runtime。
- 网络：第一阶段使用本机局域网 HTTP；第二阶段启用 HTTPS 和 Pinning。
- 数据：只使用虚构账号、虚构订单和测试 Token。

### 4.2 后端接口

```text
GET  /api/health
POST /api/login
POST /api/order
GET  /api/profile/<user_id>
WS   /ws/events
```

请求字段统一包含：

```json
{
  "user_id": "demo-user-001",
  "timestamp": 1730000000,
  "nonce": "random-16-bytes",
  "sign": "hex-hmac"
}
```

### 4.3 第一阶段签名算法

为了让签名逆向可验证，第一阶段故意使用简单规则：

```text
canonical =
    "user_id=" + user_id
    + "&timestamp=" + timestamp
    + "&nonce=" + nonce
    + "&action=" + action

sign = HMAC-SHA256(secret, canonical)
```

要求：

- 参数顺序固定。
- 编码统一为 UTF-8。
- 输出统一为小写十六进制。
- `timestamp` 允许前后 300 秒。
- `nonce` 服务端只接受一次。
- 第一阶段把 secret 硬编码在 App 中，W11 再加入简单混淆。

### 4.4 功能开关

不要一开始把所有防护都打开。按以下顺序增加：

```text
W2：HTTP 请求和明文签名
W3：Frida 可观察版本
W4：TLS Pinning
W6：简单反调试
W11：secret 拼接和简单混淆
W19：越狱检测和反分析
```

### 4.5 后端目录建议

```text
SignDemo/backend/
├── app.py
├── requirements.txt
├── config.example.py
├── tests/
│   ├── test_health.py
│   ├── test_signature.py
│   └── test_replay.py
└── README.md
```

后端必须提供：

- 健康检查。
- 登录和订单接口。
- 签名校验。
- 时间窗口校验。
- nonce 防重放。
- 开发模式日志。
- 自动化测试。

---

## 5. W1：理论基础、环境复核和工作区

### 本周目标

能解释 iOS App 的基本组成，并确认本机工具链能够支撑后续实验。

### 学习输入

- HTTP 请求/响应、Cookie、Session、Token。
- TLS 握手、证书链、代理和 ATS。
- IPA、App Bundle、Mach-O、代码签名、Provisioning Profile。
- 沙盒、Keychain、NSUserDefaults、Entitlements。
- ObjC Runtime 基本概念。
- arm64 调用约定：`x0-x7`、`sp`、`lr`。

### 周一：环境盘点

执行并记录：

```bash
sw_vers
uname -m
xcodebuild -version
xcode-select -p
brew --version
python3 --version
frida --version
ghidraRun
```

检查项目：

- Xcode 是否完整安装。
- XR 是否能被 Finder/Xcode 识别。
- 是否完成加密备份。
- 工作区是否建立。

### 周二：文件格式和签名

完成以下观察：

```bash
file /path/to/App.app/App
otool -l /path/to/App.app/App
codesign -dvvv /path/to/App.app
plutil -p /path/to/App.app/Info.plist
```

记录：

- Bundle Identifier。
- MinimumOSVersion。
- 可执行文件架构。
- Entitlements。
- 是否存在嵌入的 provisioning profile。

### 周三：创建 SignDemo 空壳

- 创建 iOS App 工程。
- 选择 XR 作为运行目标。
- 使用免费 Apple Account 自动签名。
- 在 App 中显示版本、设备和后端地址。
- 成功安装并启动一次。

### 周四：建立本地后端

- 建立 `/api/health`。
- App 调用 `/api/health`。
- 先使用局域网 HTTP，确认网络路径。
- 后端日志打印请求时间、路径和状态码。

### 周五：第一次独立复现

关闭笔记，重新完成：

```text
创建 App → 连接 XR → 启动后端 → 修改后端地址 → 编译 → 安装 → 请求 health
```

### 周末产物

- `notes/week-01-foundation.md`
- `SignDemo/README.md`
- `SignDemo/app/` 的第一个可运行版本
- `SignDemo/backend/` 的 health 接口

### 验收标准

- [ ] `xcodebuild -version` 正常。
- [ ] SignDemo 能在 XR 启动。
- [ ] App 能请求本地 `/api/health`。
- [ ] 能解释 IPA、Bundle、Mach-O 和签名的关系。
- [ ] 能解释“安装失败”和“App 启动后请求失败”属于不同层次的问题。

### 常见卡点

- Xcode 找不到 XR：检查数据线、信任电脑、开发者模式和运行目标。
- App 无法安装：先看签名和 Bundle ID，不要先怀疑后端。
- App 请求不到本机：确认手机和 Mac 在同一网络，并使用 Mac 局域网 IP。

---

## 6. W2：mitmproxy、证书链路和基础协议

### 本周目标

在自己的 SignDemo 上看到完整 HTTP/HTTPS 请求，并理解证书信任失败的原因。

### 周一：启动 mitmproxy

```bash
mitmweb --listen-port 8080
```

XR 与 Mac 连接同一 Wi-Fi，在 Wi-Fi 设置中配置手动代理：

```text
服务器：Mac 的局域网 IP
端口：8080
```

用 Safari 打开：

```text
http://mitm.it
```

安装证书后，进入：

```text
设置 → 通用 → 关于本机 → 证书信任设置
```

开启 mitmproxy 证书的完全信任。

### 周二：验证 HTTPS

- Safari 访问多个 HTTPS 站点。
- 在 mitmweb 中确认请求、响应和状态码。
- 关闭证书完全信任，再观察失败。
- 恢复信任设置。

### 周三：观察 SignDemo

为 SignDemo 增加登录请求：

```text
POST /api/login
```

记录：

- URL。
- Method。
- Headers。
- Content-Type。
- 请求 JSON。
- 响应 JSON。
- Cookie/Token。
- 失败状态码。

### 周四：编写第一个 mitmproxy addon

目标不是修改第三方流量，而是在自己的后端流量上打印字段名：

```python
from mitmproxy import http


def request(flow: http.HTTPFlow) -> None:
    if flow.request.host.endswith(".local") or "/api/" in flow.request.path:
        print(flow.request.method, flow.request.path)
        print(flow.request.headers.get("content-type", ""))
```

真实使用时按本地后端域名或 IP 收窄范围，不要把所有设备流量写入日志。

### 周五：协议笔记

分别写出：

- HTTP 明文能观察到什么。
- HTTPS 代理解密依赖什么。
- 为什么系统浏览器可以打开某站，但 App 不一定能被抓包。
- 证书错误、代理不通、Pinning 失败的区别。

### 周末产物

- `notes/week-02-mitmproxy.md`
- `scripts/mitmproxy/log_local_api.py`
- `reports/SignDemo-traffic-analysis.md` 初稿

### 验收标准

- [ ] Safari HTTPS 请求能在 mitmweb 中看到。
- [ ] SignDemo 的 HTTP 或 HTTPS 请求能看到。
- [ ] 能指出一次请求的 URL、Header、Body、Response。
- [ ] 能解释证书未安装、未信任、Pinning 三种失败。

### 常见卡点

- mitmweb 无流量：检查代理 IP、端口、防火墙和 Wi-Fi 隔离。
- HTTPS 显示连接错误：重新检查证书安装和完全信任。
- 只有 Safari 有流量：先确认 SignDemo 是否真的发起请求，再检查 App 网络配置。

---

## 7. W3：Dopamine、Frida 通道和第一个 Hook

### 本周目标

从“设备能被 Frida 看到”推进到“能对自己的 SignDemo 进行 attach、spawn 和方法 Hook”。

### 当前设备状态

- XR：A12、arm64e、iOS 18.5。
- Dopamine：已安装并激活。
- 下载 Dopamine：需要全局 VPN。
- `frida-ps -U`：已经能看到设备进程。
- `frida-ps -Uai`：本周继续确认。

### 周一：基础验收

```bash
frida-ps -U
frida-ps -Uai
```

两条命令的意义：

- `frida-ps -U`：验证 USB 设备和 Frida 通道。
- `frida-ps -Uai`：列出设备上的应用，确认目标 App 标识符。

### 周二：确认 SignDemo 标识符

```bash
frida-ps -Uai | grep -i signdemo
```

记录：

- Bundle Identifier。
- 显示名称。
- 当前是否正在运行。
- App 是否在前台。

### 周三：attach 和 spawn

先启动 App，再执行：

```bash
frida -U -n SignDemo
```

如果按 Bundle Identifier 操作：

```bash
frida -U -N com.weideshun.SignDemo
```

启动并注入：

```bash
frida -U -f com.weideshun.SignDemo
```

### 周四：ObjC Runtime 观察

在 Frida 控制台中验证：

```javascript
ObjC.available
Object.keys(ObjC.classes).filter(x => x.includes("SignDemo"))
```

编写 `scripts/frida/list_signdemo_classes.js`，输出自己的类名。

### 周五：第一个方法 Hook

目标是自己的按钮处理方法或网络请求构造方法：

```javascript
if (ObjC.available) {
  const Controller = ObjC.classes.SignDemoViewController;
  if (Controller) {
    const method = Controller["- loginButtonTapped:"];
    if (method) {
      Interceptor.attach(method.implementation, {
        onEnter(args) {
          console.log("loginButtonTapped");
          console.log(new ObjC.Object(args[0]).$className);
        }
      });
    }
  }
}
```

方法名必须以自己的工程实际类名为准。

### 周末产物

- `notes/week-03-frida.md`
- `scripts/frida/list_signdemo_classes.js`
- `scripts/frida/hook_signdemo_login.js`
- 一份 attach/spawn 输出记录

### 验收标准

- [ ] `frida-ps -U` 正常。
- [ ] `frida-ps -Uai` 能找到 SignDemo。
- [ ] 能 attach 到正在运行的 SignDemo。
- [ ] 能 spawn SignDemo。
- [ ] Hook 至少一个自己的 ObjC 方法并打印结果。

### 常见卡点

- `unable to connect to remote frida-server`：检查 Sileo 中 Frida 组件、重启后是否重新激活越狱、再运行 `frida-ps -U`。
- 找不到类：确认类名、是否 Swift 名字修饰、App 是否已运行。
- attach 后闪退：先只打印类名，不读未知指针，不修改参数。

---

## 8. W4：TLS Pinning 原理和自建 App 绕过

执行手册：[`iOS逆向实施步骤/04-W4-TLS-Pinning-详细执行步骤.md`](iOS逆向实施步骤/04-W4-TLS-Pinning-详细执行步骤.md)。

### 本周目标

在自己的 SignDemo 上理解 Pinning 的工作点，并用 Frida 观察和验证绕过效果。

### 周一：加入 Pinning

先把后端改为 HTTPS，使用本地开发证书。App 开启证书校验逻辑。

记录：

- 正常系统信任链是否通过。
- mitmproxy CA 是否被接受。
- 开启固定证书后请求如何失败。

### 周二：区分三种网络状态

建立表格：

| 状态 | App 请求 | mitmproxy | 结论 |
|---|---|---|---|
| 无代理 | 成功 | 无明文 | 网络正常 |
| 代理、无 Pinning | 成功 | 有明文 | 证书链路正常 |
| 代理、有 Pinning | 失败或拒绝 | 无明文 | 进入 Pinning 排查 |

### 周三：定位校验点

观察以下方向：

- `NSURLSessionDelegate` 的 trust 回调。
- `SecTrustEvaluateWithError`。
- 自定义证书指纹比较。
- 第三方 TLS 库的校验函数。

先静态阅读自己的代码，再用 Frida 验证调用时机。

### 周四：写最小观察脚本

脚本只针对自己的 SignDemo，第一步只记录调用，不立即修改返回值。确认调用栈和参数后，再在本地靶场验证允许请求继续。

### 周五：复测并恢复

- 关闭 Hook，确认 Pinning 能阻断代理。
- 开启 Hook，确认自己的实验流量可观察。
- 删除测试证书和临时密钥。

### 周末产物

- `notes/week-04-pinning.md`
- `scripts/frida/observe_signdemo_trust.js`
- Pinning 前后各一份脱敏流量截图。

### 验收标准

- [x] 能说明 Pinning 与系统 CA 信任的区别。
- [x] 能证明 SignDemo 的 Pinning 开关确实生效。
- [x] 能用 Frida 在自己的 App 上观察到校验点。
- [x] 能恢复到未注入状态并重新验证。

### 常见卡点

- 抓不到包不一定是 Pinning：先确认请求是否走 QUIC、是否根本没有发起。
- 脚本无输出：确认函数实际被调用，必要时先 hook 自己的 delegate 方法。
- 修改返回值后 App 闪退：检查返回类型、调用时机和线程，不要直接改未知结构体。

---

## 9. W5：WebSocket、HTTP/2、QUIC 和 gRPC

执行手册：[`iOS逆向实施步骤/05-W5-协议可见性-详细执行步骤.md`](iOS逆向实施步骤/05-W5-协议可见性-详细执行步骤.md)。

### 本周目标

理解不同协议的可见层和限制，在自己的后端上完成最小实验。

### 周一：WebSocket

- SignDemo 增加登录后 WebSocket 连接。
- 服务端每隔固定时间推送测试事件。
- 用 mitmproxy 观察握手和消息帧。
- 记录连接建立、断开和重连。

### 周二：HTTP/2

- 后端启用 HTTP/2。
- 对比 HTTP/1.1 的 Header、复用和请求顺序。
- 在 Wireshark 或代理日志中记录可观察内容。

### 周三：QUIC/HTTP3

- 先确认后端和客户端是否真的使用 UDP 443。
- 对比“启用 H3”和“强制降级到 TCP”的行为。
- 为练习建立降级开关。

抓不到时按顺序判断：

```text
是否有 UDP 流量？
是否使用了系统代理？
代理是否支持该协议？
客户端是否自动降级？
服务端是否可以关闭 H3？
```

### 周四：protobuf/gRPC

- 为自建后端增加一个简单 gRPC 方法。
- 保存 `.proto` 文件。
- 使用测试客户端生成合法请求。
- 记录字段编号、类型和序列化结果。

### 周五：协议对比

写表格：

| 协议 | 传输层 | 代理可见性 | 适合的分析方式 |
|---|---|---|---|
| HTTP/1.1 | TCP | 高 | mitmproxy |
| HTTP/2 | TCP | 取决于代理 | mitmproxy/Wireshark |
| WebSocket | TCP | 握手后看帧 | mitmproxy |
| QUIC | UDP | 工具支持有限 | 服务端降级/Wireshark |
| gRPC | HTTP/2 | 需 schema 或推断 | proto/代理扩展 |

### 周末产物

- `notes/week-05-protocols.md`
- `SignDemo/backend/proto/`
- 一份 WebSocket 帧记录。
- 一份 QUIC 降级记录。

### 验收标准

- [x] 能抓到 SignDemo 的 WebSocket 握手和帧。
- [x] 能解释 HTTP/2 多路复用。
- [x] 能解释为什么 QUIC 不等于“HTTPS 抓不到”。
- [x] 能使用 `.proto` 解释至少一个 gRPC 字段。

---

## 10. W6：SignDemo 综合验收，完成 M1

执行手册：[`iOS逆向实施步骤/06-W6-M1综合验收-详细执行步骤.md`](iOS逆向实施步骤/06-W6-M1综合验收-详细执行步骤.md)。

### 本周目标

完成一次完整的“观察流量 → 识别参数 → 复现请求”。

### 周一：补齐业务流程

完成：

```text
登录 → 获取 Token → 查询 Profile → 创建测试订单 → 接收 WS 事件
```

每个接口都生成固定测试数据。

### 周二：开启防护

依次开启：

- HTTPS。
- Pinning。
- 简单反调试检测。
- 请求时间窗口。
- nonce 防重放。

每次只开启一个开关并记录影响。

### 周三：流量分析

把每个接口写入报告：

```text
接口：
请求方式：
路径：
认证方式：
必填字段：
字段含义：
签名输入：
响应结构：
失败条件：
```

### 周四：离线复现

使用 Python 生成合法请求。脚本只连接自己的本地后端：

```text
读取测试 user_id
生成 timestamp
生成 nonce
按固定顺序拼接 canonical
计算 HMAC-SHA256
发送请求
检查响应
```

### 周五：恢复和回归

- 关闭 Frida。
- 关闭代理。
- 关闭 Pinning。
- 重新跑 App 的正常流程。
- 确认实验改动不会阻塞基础功能。

### 周末产物

- `reports/SignDemo-traffic-analysis.md`
- `scripts/reproduce/sign_login.py`
- `scripts/reproduce/sign_order.py`
- `notes/week-06-m1.md`

### M1 验收

- [x] HTTPS 明文可观察。
- [x] 能识别 Pinning 并在自己的 App 上验证绕过效果。
- [x] WebSocket 流程可解释。
- [x] HTTP/2、QUIC、gRPC 的差异有实操记录。
- [x] 离线脚本可以被自己的后端接受。
- [x] 报告不包含真实账号、真实 Token 或第三方数据。

---

## 11. W7：Frida JavaScript 基础和 ObjC Runtime

执行手册：[`iOS逆向实施步骤/07-W7-Frida-ObjC-Runtime-详细执行步骤.md`](iOS逆向实施步骤/07-W7-Frida-ObjC-Runtime-详细执行步骤.md)。

### 本周目标

从“会运行脚本”提升到“知道 hook 的对象、参数、返回值和生命周期”。

### 任务

- 复习 `ObjC.classes`、`ObjC.Object`、`ObjC.choose`。
- Hook 实例方法和类方法。
- 打印 `self`、参数和返回值。
- 练习只读观察，不修改行为。
- 为每个 Hook 写清楚目标、输入、输出和恢复方式。

### 实验顺序

```text
1. 列类
2. 列方法
3. Hook 无参数方法
4. Hook 一个对象参数方法
5. 打印返回值
6. 最后才改返回值
```

### 产物

- `scripts/frida/list_runtime_methods.js`
- `scripts/frida/hook_network_request.js`
- `notes/week-07-frida-runtime.md`

### 验收标准

- [x] 能找到自己的 ViewController。
- [x] 能区分实例方法和类方法。
- [x] 能打印一个 NSString、NSDictionary 或 NSData。
- [x] 能解释 Hook 导致 App 闪退的两个原因。

---

## 12. W8：Native Hook 和 UnCrackable L1

执行手册：[`iOS逆向实施步骤/08-W8-Native-Hook-UnCrackable-L1-详细执行步骤.md`](iOS逆向实施步骤/08-W8-Native-Hook-UnCrackable-L1-详细执行步骤.md)。

### 本周目标

完成第一个授权靶场的动态分析闭环。

### UnCrackable 安装路线

当前使用源码工程：

```text
https://github.com/OWASP/mas-crackmes
iOS/Level1/UnCrackable Level 1.xcodeproj
```

基本步骤：

1. 用 Xcode 打开工程。
2. 在 Project 和 Target 的 Signing & Capabilities 中选择自己的免费 Apple Account。
3. 把 Bundle Identifier 改为自己的唯一值，例如 `com.weideshun.uncrackable1`。
4. 运行目标选择 XR。
5. 如果出现 `libarclite` 错误，把 Project 和 Target 的 Debug/Release 的 iOS Deployment Target 统一改为 iOS 18.0。
6. `Product → Clean Build Folder`。
7. 再编译安装。

不要直接依赖原始过期 IPA。不要把 `sg.vp.UnCrackable1` 作为免费签名时的固定 Bundle ID。

### 周一至周二：静态预览

- 阅读 `UnDebuggable/` 源码。
- 找到 root 检测、调试检测和密钥校验相关代码。
- 记录类名、方法名、字符串和调用关系。

### 周三：运行观察

```bash
frida-ps -Uai | grep -i uncrackable
frida -U -f com.weideshun.uncrackable1
```

实际 Bundle Identifier 以 Xcode 中的值为准。

### 周四：Hook 检测点

- 先打印调用。
- 再验证返回值和 UI 行为。
- 最后才在自己的靶场中修改返回值。

### 周五：密钥校验

- 找到输入框提交路径。
- 记录输入如何进入校验方法。
- 记录成功和失败分支。

### 周末产物

- `reports/uncrackable-l1.md`
- `scripts/frida/uncrackable_l1_observe.js`
- 一张调用链草图。

### 验收标准

- [x] 源码版 App 能在 XR 启动。
- [x] 能找到并解释至少一个检测点。
- [x] 能通过动态观察确认检测点确实执行。
- [x] 能解释“安装失败”和“靶场逻辑失败”的区别。

---

## 13. W9：Mach-O、代码签名和 Ghidra

执行手册：[`iOS逆向实施步骤/09-W9-Mach-O-Ghidra-详细执行步骤.md`](iOS逆向实施步骤/09-W9-Mach-O-Ghidra-详细执行步骤.md)。

### 本周目标

能把一个 App 从 Bundle 结构带到 Ghidra，并知道反编译结果哪些可信、哪些需要验证。

### 任务

- 观察 Mach-O Header、Load Commands 和架构。
- 使用 `strings`、`nm`、`otool` 做初筛。
- Ghidra 导入自己的 SignDemo 二进制。
- 对比 ObjC 方法名、字符串和交叉引用。
- 理解 Swift 名字修饰和优化对阅读的影响。

### 常用命令

```bash
file App
otool -hv App
otool -l App
nm -m App
strings App | grep -i sign
codesign -dvvv App
```

### Ghidra 操作顺序

```text
创建 Project
导入 Mach-O
确认架构
运行 Auto Analyze
搜索字符串
查看引用
定位函数
重命名变量
记录推断依据
```

### 产物

- `notes/week-09-mach-o.md`
- `reports/signdemo-static-location.md`
- Ghidra Project 的本地备份说明。

### 验收标准

- [x] 能从 App Bundle 找到主 Mach-O。
- [x] 能解释 `cryptid`、Load Commands 和架构。
- [x] 能在 Ghidra 中定位至少一个与签名流程相关的函数或字符串。
- [x] 能指出一处需要动态验证的反编译推断。

---

## 14. W10：脱壳概念和静态定位

执行手册：[`iOS逆向实施步骤/10-W10-脱壳概念-静态动态-详细执行步骤.md`](iOS逆向实施步骤/10-W10-脱壳概念-静态动态-详细执行步骤.md)。

### 本周目标

理解 FairPlay 加密和脱壳边界，并把静态定位与 Frida 观察对上。

### 任务

- 对比自建 App 与商店 App 的 Mach-O 状态。
- 理解 `cryptid` 和加密段。
- 只对自己有权分析的 App 或授权靶场进行脱壳练习。
- 选择当前仍维护的 `frida-ios-dump` 版本，先查看其兼容说明。
- 不把脱壳脚本直接用于第三方生产 App。

### 实验重点

```text
字符串初筛
→ 方法/符号定位
→ 交叉引用
→ Frida 运行时验证
→ 回到 Ghidra 修正命名和推断
```

### 产物

- `notes/week-10-static-dynamic.md`
- `reports/signdemo-static-dynamic.md`

### 验收标准

- [x] 能解释为什么自建未加密 App 不能代表商店 App 的脱壳过程。
- [x] 能在静态位置和动态调用之间建立对应关系。
- [x] 能说明 Ghidra 伪代码不是原始源码。

---

## 15. W11：签名算法定位与混淆版本

执行手册：[`iOS逆向实施步骤/11-W11-签名混淆-详细执行步骤.md`](iOS逆向实施步骤/11-W11-签名混淆-详细执行步骤.md)。

### 本周目标

把 SignDemo 的明文 secret 改成简单混淆，并重新完成定位。

### App 改造

- 将 secret 拆成多个常量。
- 加入固定字节变换。
- 保持最终 HMAC 算法和接口不变。
- 保留 Debug 开关，方便和原始版本对比。

### 分析步骤

```text
抓一组已知输入输出
→ 在 App 中 Hook HMAC 输入
→ 在 Ghidra 搜索常量和调用点
→ 对照函数参数
→ 验证动态输入输出
→ 在 Python 中复现
```

### 产物

- `scripts/frida/capture_hmac_input.js`
- `scripts/reproduce/sign_v2.py`
- `notes/week-11-signature-location.md`

### 验收标准

- [x] 能从一次请求获得 canonical、secret 派生结果和 sign。
- [x] 能在 Ghidra 中定位签名生成附近的函数。
- [x] Python 脚本能生成与 App 一致的 sign。

---

## 16. W12：签名逆向综合，完成 M2

执行手册：[`iOS逆向实施步骤/12-W12-M2签名逆向综合-详细执行步骤.md`](iOS逆向实施步骤/12-W12-M2签名逆向综合-详细执行步骤.md)。

### 本周目标

独立完成一次完整签名逆向，并形成可以交给别人复查的报告。

### 报告结构

```text
1. 对象和授权范围
2. 接口和样本
3. 观察到的输入输出
4. 静态定位过程
5. 动态 Hook 过程
6. canonical 规则
7. HMAC 或其他算法
8. nonce 和时间窗口
9. 离线复现脚本
10. 服务器侧修复建议
11. 限制和未验证项
```

### 综合验收

- [x] 不看旧脚本也能重新定位签名入口。
- [x] 能解释一次请求从按钮到网络发送的调用链。
- [x] 能用离线脚本得到服务端接受的结果。
- [x] 改动时间戳或 nonce 后服务端能拒绝。
- [x] 报告只涉及自己的 App 和后端。

产物：

- `reports/signature-reproduction.md`
- `scripts/reproduce/signature_v2.py`
- `notes/week-12-m2.md`

---

## 17. W13：LLDB、断点和崩溃定位

执行手册：[`iOS逆向实施步骤/13-W13-LLDB-断点-详细执行步骤.md`](iOS逆向实施步骤/13-W13-LLDB-断点-详细执行步骤.md)。

### 本周目标

掌握调试器的基本观察能力，为后续 native 分析做准备。

### 任务

- 认识 Xcode 调试器与 LLDB。
- 在自己的 SignDemo 中设置源码断点。
- 使用符号断点和方法断点。
- 观察寄存器、栈和返回值。
- 保存一次可复现崩溃和符号化记录。

### 常用命令

```text
breakpoint set -n "-[Class method:]"
register read
po $x0
bt
thread backtrace
```

命令需根据当前进程和符号实际调整。

### debugserver 说明

越狱设备上的 debugserver、entitlement 和 rootless 路径容易随 Dopamine 版本变化。先完成 Xcode 本地调试，再按当前环境的官方/社区兼容说明处理设备侧 debugserver，不把不确定的旧命令写成固定步骤。

### 验收标准

- [x] 能在自建 App 中停在源码断点。
- [x] 能查看调用栈。
- [x] 能解释 `x0` 在 arm64 调用约定中的常见用途。
- [x] 能保存一份脱敏崩溃记录。

---

## 18. W14：Theos 和最小 Tweak

执行手册：[`iOS逆向实施步骤/14-W14-Theos-最小Tweak-详细执行步骤.md`](iOS逆向实施步骤/14-W14-Theos-最小Tweak-详细执行步骤.md)。

### 本周目标

理解越狱插件从源码、编译到安装的完整流程。

### 任务

- 了解 Theos、Logos 和 rootless 构建。
- 只针对自己的 SignDemo 写 Hook。
- 第一个 Tweak 只打印生命周期日志。
- 第二个 Tweak 再观察一个方法参数。
- 不直接修改系统 App。

### 产物

- `notes/week-14-theos.md`
- `SignDemoTweak/`
- 一份安装、卸载和恢复记录。

### 验收标准

- [x] 能编译出 deb。
- [x] 能在测试设备安装。
- [x] 能看到自己的日志。
- [x] 能卸载并恢复原始 App 行为。

---

## 19. W15：DVIA-v2 和客户端存储

执行手册：[`iOS逆向实施步骤/15-W15-DVIA-客户端存储-详细执行步骤.md`](iOS逆向实施步骤/15-W15-DVIA-客户端存储-详细执行步骤.md)。

### 本周目标

开始漏洞研究，但仍然限定在授权靶场和自建 App。

### 练习项目

- DVIA-v2。
- 自建 InsecureApp。

### 第一组问题

- NSUserDefaults 保存敏感信息。
- plist 和日志泄露。
- Keychain 配置错误。
- 本地“已登录”状态可被直接修改。

### 每个问题都要记录

```text
资产：
前置条件：
复现步骤：
观察证据：
影响：
根因：
修复建议：
修复后回归结果：
```

### 产物

- `reports/dvia-storage.md`
- `reports/insecureapp-storage.md`

### 验收标准

- [x] 能在靶场中定位至少两类不安全存储。
- [x] 能区分“可读”与“可导致安全影响”。
- [x] 每个结论都有复现证据。

---

## 20. W16：URL Scheme、WebView 和本地认证

执行手册：[`iOS逆向实施步骤/16-W16-URL-WebView-本地认证-详细执行步骤.md`](iOS逆向实施步骤/16-W16-URL-WebView-本地认证-详细执行步骤.md)。

### 本周目标

理解客户端入口和本地信任边界。

### 任务

- 枚举自建 App 的 URL Scheme。
- 检查是否校验来源和参数。
- 观察 WebView 与 JavaScript Bridge。
- 设计一个本地认证绕过实验。
- 用修复版本重新验证。

### 产物

- `reports/dvia-url-scheme.md`
- `reports/insecureapp-webview.md`
- `notes/week-16-client-boundary.md`

### 验收标准

- [x] 能说明 URL Scheme 的攻击面。
- [x] 能说明 WebView Bridge 的信任边界。
- [x] 能写出至少一条具体修复建议并验证。

---

## 21. W17：API 业务逻辑和重放

执行手册：[`iOS逆向实施步骤/17-W17-API重放-详细执行步骤.md`](iOS逆向实施步骤/17-W17-API重放-详细执行步骤.md)。

### 本周目标

把前面掌握的抓包能力用于自建后端业务逻辑测试。

### 自建缺陷开关

在本地后端提供开发分支，逐一打开：

- 缺失签名校验。
- nonce 不防重放。
- `user_id` 只信任客户端传入。
- 缺少对象级权限检查。
- 缺少请求频率限制。

### 测试顺序

```text
正常请求
→ 只改 user_id
→ 只改 timestamp
→ 重放完全相同请求
→ 删除 sign
→ 改 action
→ 恢复安全版本
```

### 产物

- `reports/local-api-replay.md`
- `scripts/reproduce/replay_local_request.py`

### 合规限制

真实 App 的生产请求不能重放、改包或探测。即使只改一个字段，也属于对第三方后端的未授权测试。本周所有操作只针对自己的本地后端。

---

## 22. W18：越权、IDOR 和修复验证

执行手册：[`iOS逆向实施步骤/18-W18-越权-IDOR-详细执行步骤.md`](iOS逆向实施步骤/18-W18-越权-IDOR-详细执行步骤.md)。

### 本周目标

从“发现参数可改”提升到“证明服务端授权缺失”。

### 任务

- 创建两个虚构用户。
- 为两个用户创建不同订单。
- 用用户 A 的 Token 请求用户 B 的对象。
- 在服务端加入对象级权限校验。
- 重新执行相同测试。

### 报告必须区分

- 客户端页面隐藏不等于权限控制。
- 改参数成功不一定代表越权，要确认访问到了不属于当前用户的数据。
- HTTP 200 不一定代表业务成功，要检查响应语义。

### 产物

- `reports/local-api-idor.md`
- `notes/week-18-business-logic.md`

### 验收标准

- [x] 能构造两个独立测试用户。
- [x] 能证明修复前后的差异。
- [x] 报告中包含服务端修复代码位置和回归结果。

---

## 23. W19：反调试、越狱检测和反分析

执行手册：[`iOS逆向实施步骤/19-W19-反调试-越狱检测-详细执行步骤.md`](iOS逆向实施步骤/19-W19-反调试-越狱检测-详细执行步骤.md)。

### 本周目标

理解常见防护的检测面，并在自己的 App 上完成观察和对抗实验。

### 学习范围

- 文件路径检测。
- URL Scheme 检测。
- 动态库和环境变量检测。
- `ptrace`、`sysctl` 等反调试思路。
- 字符串拆分和简单混淆。
- 方法名、符号和日志泄露。

### 实验顺序

```text
先写明文防护
→ 用 Frida 观察检测点
→ 记录误报和漏报
→ 加入简单混淆
→ 重新定位
→ 比较防护成本和实际收益
```

不要把“绕过成功”当成安全性结论。最终报告要讨论服务端是否仍然信任客户端。

### 产物

- `reports/anti-analysis-lab.md`
- `scripts/frida/observe_jailbreak_checks.js`
- `notes/week-19-defenses.md`

### 验收标准

- [ ] 能列出至少三类越狱检测面。
- [ ] 能解释反调试和代码签名不是同一层防护。
- [ ] 能说明客户端防护不能替代服务端授权。

---

## 24. W20：复盘、报告和下一季度路线

### 本周目标

把零散实验整理成可展示、可复现、可继续扩展的作品集。

### 复盘内容

- M0-M3 是否全部通过。
- 哪些命令能独立执行。
- 哪些工具仍需查资料。
- 哪些失败可以快速定位。
- 哪些结论只在自建环境成立。

### 最终产物

- `reports/SignDemo-traffic-analysis.md`
- `reports/signature-reproduction.md`
- `reports/uncrackable-l1.md`
- `reports/dvia-storage.md`
- `reports/local-api-replay.md`
- `reports/local-api-idor.md`
- `reports/anti-analysis-lab.md`
- `notes/quarter-review.md`

### 下一季度三选一

1. **iOS 应用安全审计**：继续 API 业务逻辑、客户端存储和 WebView。
2. **系统与越狱研究**：补 arm64、内核、Mach、调试和公开漏洞分析。
3. **移动端恶意样本分析**：补行为分析、沙箱、持久化和检测工程。

### M3 验收

- [ ] 至少 3 份完整漏洞报告。
- [ ] 至少 1 份签名逆向报告。
- [ ] 至少 1 个可重复的本地后端。
- [ ] 至少 5 个可复用 Frida/mitmproxy 脚本。
- [ ] 能在不依赖旧笔记的情况下演示一次完整闭环。

---

## 25. 日常验收命令

### 25.1 设备和 Frida

```bash
frida-ps -U
frida-ps -Uai
frida -U -f com.weideshun.SignDemo
frida-trace -U -i "*NSURLSession*" com.weideshun.SignDemo
```

每条命令的目的：

- `frida-ps -U`：设备连接和 Frida 通道。
- `frida-ps -Uai`：应用列表和标识符。
- `frida -U -f`：启动并注入。
- `frida-trace`：快速观察指定符号或方法族。

### 25.2 工程和签名

```bash
xcodebuild -version
xcode-select -p
codesign -dvvv /path/to/App.app
plutil -p /path/to/App.app/Info.plist
```

### 25.3 Ghidra 和 Mach-O

```bash
file /path/to/App
otool -hv /path/to/App
otool -l /path/to/App
strings /path/to/App | head
```

---

## 26. 排障树

### 26.1 `frida-ps -U` 无设备

```text
确认 USB 连接
→ Finder 是否识别 XR
→ 是否点过“信任此电脑”
→ Dopamine 是否在本次重启后重新激活
→ Sileo 中 Frida 组件是否正常
→ 重新运行 frida-ps -U
```

不要先重装所有工具。先判断是 USB、越狱状态、设备侧组件还是 Mac 客户端。

### 26.2 设备重启后 Frida 失效

- Dopamine 越狱通常需要在重启后重新激活。
- 先打开 Dopamine 并重新执行 Jailbreak。
- 等待系统回到正常状态后再运行 `frida-ps -U`。
- 不要在 Frida 客户端报错时立刻修改 App。

### 26.3 Sileo 源无法访问

- 先确认 Mac/手机网络是否需要全局 VPN。
- 不要连续添加来路不明的镜像源。
- AppSync 官方源当前不作为主线依赖。
- Frida 源和 Dopamine 下载问题分别排查，不要混为一谈。

### 26.4 App 安装签名失败

```text
看错误是 profile 过期
→ 还是 Bundle ID 不匹配
→ 还是设备 UDID 不在 profile
→ 还是部署版本/架构不兼容
```

`This provisioning profile has expired` 表示签名材料过期，不是 Frida 问题，也不是“手机没越狱”。源码编译版应让 Xcode 自动生成新的签名。

### 26.5 Xcode 报 `libarclite`

处理顺序：

1. 左侧蓝色工程图标。
2. 检查 PROJECT 的 Build Settings。
3. 检查 TARGETS 的 Build Settings。
4. 搜索 `iOS Deployment Target`。
5. Debug 和 Release 统一改为 iOS 18.0。
6. 不手动填写旧的 `ARCHS` 或 `VALID_ARCHS`。
7. `Product → Clean Build Folder`。
8. 重新选择 XR 编译。

### 26.6 mitmproxy 没有 HTTPS 明文

```text
确认手机代理地址和端口
→ 确认证书已安装
→ 确认证书完全信任
→ 确认 App 确实发起请求
→ 判断是否 Pinning
→ 判断是否 QUIC/HTTP3
```

### 26.7 App 有 Pinning

- 先在自己的 App 源码中确认校验点。
- 再用 Frida 做观察。
- 先记录调用，不要一上来替换所有安全函数。
- 只在自建 App 和授权靶场执行绕过实验。

### 26.8 Frida attach 后 App 闪退

- 先不修改返回值。
- 只打印类名或固定字符串。
- 检查是否把错误的参数当成 ObjC 对象。
- 检查 Hook 的函数签名和返回类型。
- 改用 spawn 后再 attach。
- 保存设备日志，定位是脚本错误还是 App 自身崩溃。

### 26.9 ObjC 类找不到

- App 是否正在运行。
- 类是否为 Swift 类型。
- 是否使用了模块名前缀或名字修饰。
- 是否被优化或裁剪。
- 先枚举包含关键词的类，再决定 Hook 目标。

### 26.10 Ghidra 伪代码不可靠

- 反编译结果只是推断。
- 用字符串引用、交叉引用和动态调用互相验证。
- 注意优化、内联、符号缺失和 Swift 名字修饰。
- 不要只凭变量名下结论。

---

## 27. 合规边界

### 允许

- 自己的 XR、自己的 Mac。
- 自己编写的 SignDemo、InsecureApp 和本地后端。
- OWASP UnCrackable、DVIA 等授权靶场。
- 对自己 App 的本地存储、进程、网络和代码进行观察。

### 不做

- 对第三方生产 API 重放、改包、探测或绕过认证。
- 使用真实账号、真实支付数据或真实 Token 做练习。
- 发布或传播第三方 App 的破解包。
- 把抓到的真实用户数据写入报告或脚本。
- 把 AppSync、Frida 或越狱工具安装到不属于自己的设备。

### 报告中的最小授权声明

```text
本实验仅针对本人拥有的设备、自建 App、自建后端及公开授权靶场。
所有请求重放、参数修改和安全防护绕过均在本地实验环境完成。
```

---

## 28. 每周复盘模板

```markdown
# Week __ 复盘

日期：
设备：
App：

## 本周目标

## 已完成
- [ ]
- [ ]
- [ ]

## 验收证据
- 命令：
- 截图：
- 日志：
- 报告：

## 失败记录
现象：
原因：
排查顺序：
最终解决：

## 仍未解决

## 下周三件事
1.
2.
3.

## 下周唯一最重要的结果

## 合规确认
本周是否只操作自己的设备、自建项目和授权靶场：是 / 否
```

---

## 29. 第一周执行清单

从当前状态继续时，不再重复已经完成的 Dopamine 安装。下一步按以下顺序执行：

```text
1. 启动并确认后端
2. 用 Xcode 在 XR 上运行 SignDemo
3. 执行 frida-ps -Uai
4. 记录 SignDemo 的真实 Bundle Identifier
5. 执行 frida -U -f <bundle-id>
6. 完成第一个只读 Hook
7. 用 mitmproxy 观察 SignDemo 的 health/login 请求
8. 把输出写入 notes/week-03-frida.md
```

本计划的下一项实际工作不是继续安装工具，而是让 SignDemo 形成“App、后端、抓包、Frida”四者之间的第一个可重复闭环。
